---
sidebar_position: 1
---

import DocumentLink from '../src/components/DocumentSection/DocumentSection';
import style from '../src/components/DocumentSection/styles.module.css';

# Topics

:::danger Construction

The documentation is currently still in progress. Missing pages will be added in the near future.<br />
The progress can be viewed on [GitHub](https://github.com/dulliag/documentation/).

:::

export const DocumentSection = () => {
  return (
    <div>
      <div className={style.grid}>
        {[
          {
            show: true,
            label: 'LoggerJS',
            description: null,
            targetLocation: 'logger-js/logger-js-client',
          },
          {
            show: false,
            label: 'LoggerJS-Web',
            description: null,
            targetLocation: 'logger-js/logger-js-web',
          },
          {
            show: true,
            label: '@dulliag/components',
            description: null,
            targetLocation: 'dulliag-components',
          },
          {
            show: true,
            label: '@dulliag/discord-helper',
            description: null,
            targetLocation: 'dulliag-components',
          },
          {
            show: true,
            label: 'DulliBot',
            description: null,
            targetLocation: 'dullibot',
          },
          {
            show: false,
            label: 'A3RLRPG-Infoapp',
            description: null,
            targetLocation: 'dullibot',
          },
        ]
          .filter((link) => link.show)
          .map((link, i) => (
            <div key={i}>
              <DocumentLink {...link} key={link.targetLocation} />
            </div>
          ))}
      </div>
    </div>
  );
};

<DocumentSection />
